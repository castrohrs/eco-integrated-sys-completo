
# EcoLog Enterprise — eco-integrated-sys-completo

Este é o sistema central de inteligência operacional da **EcoLog**, configurado para o repositório oficial.

## 🚀 Como enviar para o GitHub

Para subir este projeto para o repositório `eco-integrated-sys-completo`, abra o terminal na pasta do projeto e execute os seguintes comandos:

```bash
# 1. Inicializar o Git
git init

# 2. Adicionar todos os arquivos
git add .

# 3. Criar o primeiro commit
git commit -m "Initial commit: EcoLog Enterprise Core v5.2"

# 4. Configurar o login (se necessário)
git config --global user.email "castrohrs@gmail.com"
git config --global user.name "CastroHRS"

# 5. Conectar ao repositório remoto
git remote add origin https://github.com/castrohrs/eco-integrated-sys-completo.git

# 6. Enviar para o repositório (Main)
git branch -M main
git push -u origin main
```

## 🛠️ Tecnologias
- **Frontend**: React 19 + TypeScript
- **Estilização**: Tailwind CSS (SaaS Modern Style)
- **IA**: Google Gemini API (OCR, Análise Preditiva, Geração de Mídia)
- **Realtime**: Socket.io + BroadcastChannel
- **Módulos Técnicos**: Radar Tático GPS, Simulador CT-e, Centro de Documentação A4.

---
**Mantenedor:** castrohrs@gmail.com
